<h1> ➕ Add bookmark</h1>
<%= simple_form_for @bookmarks do |f| %>
        <%= f.input :comment %>
        <%= f.submit "Create", class: "btn btn-primary" %>
      <% end %>
      <%= link_to 'Back', bookmarks_path, class: 'btn btn-info' %>

<%= button_to "Destroy this bookmark", @bookmarks, method: :delete %>
