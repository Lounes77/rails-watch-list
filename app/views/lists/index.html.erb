<div class="container">
  <h1>Lists</h1>
  <p>All lists movies.</p>
  <ul>
    <% @lists.each do |list|  %>
      <li> <%= link_to list.name, list_path(list) %></li>
    <% end %>
  </ul>
</div>

<div>
  <%= link_to "Create list", new_list_path %>
</div>
